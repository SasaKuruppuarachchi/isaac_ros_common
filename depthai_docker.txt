#
RUN apt-get update && apt-get install -y wget build-essential cmake pkg-config libjpeg-dev libtiff5-dev libavcodec-dev libavformat-dev libswscale-dev libv4l-dev libxvidcore-dev libx264-dev libgtk2.0-dev libgtk-3-dev libatlas-base-dev gfortran git libopencv-dev

ADD depthai-python/ci/docker_dependencies.sh .
RUN ./docker_dependencies.sh

RUN pip install -U pip && pip install --extra-index-url https://www.piwheels.org/simple/ --prefer-binary opencv-python 

# Copy over the files
# COPY depthai-python/ /depthai-python

# # Install C++ library
# RUN set -e; \
#                 # Remove any potentially broken OpenCV config installed at /usr/lib/cmake/opencv4
#                 rm -rf /usr/lib/cmake/opencv4 || true; \
#                 # Prefer pkg-config to locate the correct OpenCV CMake package dir
#                 OPENCV_DIR=$(pkg-config --variable=cmakedir opencv4 2>/dev/null || true); \
#                 if [ -z "$OPENCV_DIR" ] || [ ! -d "$OPENCV_DIR" ]; then \
#                     if [ -d /usr/lib/x86_64-linux-gnu/cmake/opencv4 ]; then OPENCV_DIR=/usr/lib/x86_64-linux-gnu/cmake/opencv4; \
#                     elif [ -d /usr/lib/aarch64-linux-gnu/cmake/opencv4 ]; then OPENCV_DIR=/usr/lib/aarch64-linux-gnu/cmake/opencv4; \
#                     elif [ -d /usr/lib/arm64-linux-gnu/cmake/opencv4 ]; then OPENCV_DIR=/usr/lib/arm64-linux-gnu/cmake/opencv4; \
#                     else OPENCV_DIR=/usr/lib/cmake/opencv4; fi; \
#                 fi; \
#                 echo "Using OpenCV_DIR=$OPENCV_DIR"; \
#                 cmake -S /depthai-python/depthai-core -B /build \
#                         -D CMAKE_BUILD_TYPE=Release \
#                         -D BUILD_SHARED_LIBS=ON \
#                         -D CMAKE_INSTALL_PREFIX=/usr/local \
#                         -D OpenCV_DIR="$OPENCV_DIR" && \
#                 cmake --build /build --parallel 4 --config Release --target install

# # Install Python library
# RUN cd /depthai-python && python3 -m pip install .
